apiVersion: v1
kind: Service
metadata:
  name: demo-api-bg-svc
  namespace: lab32
spec:
  type: NodePort
  selector:
    app: demo-api
    slot: blue
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30081
